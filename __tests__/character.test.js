import { TestScheduler } from 'jest-cli'
import Character from '../src/js/character.js'

describe('Character', () => {
  test('should create a Character object with property type', () => {
    let newCharacter = new Character();
    expect(newCharacter)

  })
})




